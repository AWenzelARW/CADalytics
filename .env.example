# CADalytics Creator Factory Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration (for iframe embedding)
CORS_ORIGIN=*
ALLOW_CREDENTIALS=true

# Security Headers
X_FRAME_OPTIONS=ALLOWALL
CSP_FRAME_ANCESTORS=*

# Session Configuration
SESSION_TIMEOUT=3600000
MAX_SESSIONS=10000

# Optional: Database Configuration (if upgrading from in-memory storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/cadalytics
# DB_POOL_SIZE=10